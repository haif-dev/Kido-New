-- Seed data for local dev. Run with: pnpm db:reset
-- (Service-role bypasses RLS, so this seed works regardless of policies.)

-- Example: a few wilayas pre-populated as profile cities for testing search.
-- Real users will be created through the signup flow.
